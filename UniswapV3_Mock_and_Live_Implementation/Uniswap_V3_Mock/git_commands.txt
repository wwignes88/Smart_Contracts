git ls-files -s
git ls-tree HEAD
git cat-file -p master^{tree}


git log --oneline --decorate --graph --all it will print out the history of your commits, showing where your branch pointers are and how your history has diverged.

git checkout -b iss53   # create and move into branch 
git branch -D <testing> # delete branch

git remote add <remote name> <repo link>
git remote -v
git remote show  <remote>

git push <remote> <branch>
git push --set-upstream essays public

git submodule add <sub module repo url>